double tau=.025;

Section (Parallel)
{
  int ProcsPerClone=1;

}
Section (System)
{
  bool OpenLoops=false;
  int NumTimeSlices=28; 
  Array<double,1> Box(3)=[15.73612599,15.73612599, 15.73612599];
  Array<bool, 1 > IsPeriodic(3)=[true,true,true];
  Section (Particles)
  {
    Section (Species)
    {
      string Name="He";
      string Type="He";
      double lambda=6.059615; 
      string Statistics="BOSON";
      int NumParticles=96;
      int NumDim=3;
      string InitPaths="FILE";
      string File="hehpEquilibriumRunv2.0.h5";
    }
  }
}
Section (Action)
{
  int MaxLevels = 2;
  Array<string,1> PairActionFiles(1) = ["../../PairActions/He495.PairAction"];
}

Section (Observables)
{
  string OutFileBase = "hehpEquilibriumRunv2";
  Section (Observable)
    {
      string type="StructureFactor";
      string Name="StructureFactor";
      string Species1 = "He";
      string Species2 = "He";
      string Description="Helium-Helium Structure Factor";
      int freq=2;
      int dumpFreq=20;
    }
  
  Section (Observable)
    {
      string type = "Sign";
      string Name = "Sign";
      int freq=1;
      int dumpFreq=10;
    }

  Section (Observable)
    {
      string type = "Energy";
      string Name = "Energy";
      string Description="Total Energy";
      int freq=2;
      int dumpFreq=20;
      	
    }
  Section (Observable)
    {
      string type = "PairCorrelation";
      string Name = "HeHePC";
      string Species1 = "He";
      string Species2 = "He";
      string Description="Helium-Helium Pair Correlation";
      int freq=2;
      int dumpFreq=20;
      Section (Grid)
	{
	  string type = "Linear";
	  double start = 0.0;
	  int NumPoints = 100;
	}
    }
  Section (Observable)
    {
      string type="PathDump";
      string Name="PathDump";
      int freq=2;
      int dumpFreq=2;
    }

}


Section (Moves){

  Section (Move)
    {
      string type="BisectionBlock";
      string name="NormalBisection";
      Array<int,1> ActiveSpecies(1)=[0]; //This needs to be prettier input
      ///here should be electron
      int NumParticlesToMove=1;
      int NumLevels=2;
      string Species="He";
      int StepsPerBlock=50;
      string PermuteType="TABLE";
      Array<double,1> Gamma(4)=[1.0,1.0,1.0,1.0];
      double epsilon=1e-3;
    }
   Section (Move)
    {
      string type="Bisection";
      string name="abNormalBisection";
      Array<int,1> ActiveSpecies(1)=[0]; //This needs to be prettier input
      ///here should be electron
      int NumParticlesToMove=1;
      string NumLevels="Max";
            
    }
  Section (Move)
    {
      string type="ShiftMove";
      string name="Shift";
    }
  Section (Move)
    {
      string type="CycleBlock";
      string name="Permutation";
      int NumLevels=2;
      Array<double,1> Gamma(4) = [1.0, 1.0, 1.0, 1.0];
      double epsilon=1e-3;
      int Steps=400;
      string Species="He";
    }
}


Section (Algorithm)
{
  Section (Loop){ // Before Accumulating Observables
    int steps=100; 
    Section (Loop){ 

      int steps=3;
      Section (Move) {string name="NormalBisection";} 
      Section (Loop){
	int steps=8; 
	Section (Move) {string name="abNormalBisection";}
	
      }

    }
    Section (Move) {string name = "Shift";}
  }
  Section (Loop){ //Start Accumulating observables
    int steps=3000000000;
    Section (Loop){
      int steps=3;
      Section (Move) {string name="NormalBisection";}
      Section (Loop){
	int steps=8; 
	Section (Move) {string name="abNormalBisection";} 
      }

    }
    Section (Observe) {string name = "HeHePC";}
    Section (Observe) {string name = "Energy"; }
    Section (Observe) {string name = "PathDump"; }
    //    Section (Observe) {string name = "Sign"; }
    Section (Move) {string name = "Shift"; }
  }
}
	  
