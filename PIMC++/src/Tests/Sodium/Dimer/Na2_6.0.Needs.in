double tau=0.5;

Section (Parallel) {
  int ProcsPerClone = 1;
}

Section (System)
{
  int NumTimeSlices=600;
  Array<double,1> Box(3)=[200.0,200.0,200.0];
  Array<bool,1> IsPeriodic(3)=[true, true, true];
  //  double kCutoff = 0.15;
  Section (Particles)
  {
    Section (Species)
    {
      string Name="e";
      string Type="e";
      double lambda=0.5;
      string Statistics = "BOLTZMANNON";
      string NodeType   = "NONE";
      int NumParticles  = 2;
      int NumDim        = 3;
      string InitPaths  = "LEVIFLIGHT";
      Array<double,2> Positions(2,3)=[-0.3, 0.0, 0.0, 
				       0.3, 0.0, 0.0];
    }
    Section (Species)
    {
      string Name="Na+";
      string Type="Na+";
      double lambda=0.0; //classical particle
      string Statistics="BOLTZMANNON";
      int NumParticles=2;
      int NumDim=3;
      string InitPaths="FIXED";
      Array<double,2> Positions(2,3)=[-3.0,  0.0, 0.0, 
				       3.0,  0.0, 0.0 ];
    }
  }
}

Section (Action)
{
  int MaxLevels = 6;
  bool UseRPA = false;
  int NumBreakupKnots = 20;
  int NumImages = 0;
  bool UseLongRange = false;
  Array<string,1> PairActionFiles(3) = 
    ["~/PairActions/Na_HF_NLPP.beta16.0.tricubicfit2.h5",
     "~/PairActions/e-e.fit.beta16.0.h5",
     "~/PairActions/Na-Na.PairAction"];
  bool UseBackground = false;
}

Section (Observables)
{
  string OutFileBase = "Na2_6.0_tau0.5_beta300.Needs";
  Section (Observable) {
    string Type = "Energy";
    string Name = "Energy";
    int Frequency = 10;
    int dumpFreq=500;
  }
  Section (Observable) {
    string Type = "PairCorrelation";
    string Name = "e-NaPC";
    string Species1 = "e";
    string Species2 = "Na+";
    int Frequency = 10;
    int dumpFreq = 500;
    Section (Grid)
      {
	string Type = "Linear";
	double start = 0.0;
	double end = 6.0;
	int NumPoints = 200;
      }
  }
}

Section (Moves){
  Section (Move)
  {
    string Type="BisectionBlock";
    string Name="eBlock";
    string PermuteType = "NONE";
    string Species = "e";
    int StepsPerBlock = 4;
    int NumLevels=6;
  }
  Section (Move)
  {
    string Type="ShiftMove";
    string Name="Shift";
  }
}


Section (Algorithm)
{
  Section (Loop){ // Before Accumulating Observables
    int Steps=1000; 
    Section (Move) {string Name="eBlock";}
    Section (Move) {string Name = "Shift";}
  }
  Section (Loop){ //Start Accumulating observables
    int Steps=30000000;
    Section (Loop) {
      int Steps = 5000;
      Section (Move)      { string Name="eBlock";   } 
      Section (Move)      { string Name = "Shift";  }
      Section (Move)      { string Name="eBlock";   } 
      Section (Move)      { string Name = "Shift";  }
      Section (Move)      { string Name="eBlock";   } 
      Section (Move)      { string Name = "Shift";  }
      Section (Observe)   { string Name = "e-NaPC"; }
      Section (Observe)   { string Name = "Energy"; }
    }
    Section   (WriteData) {                         }
    //Section (Observe) {string Name = "PathDump";}
  }
}
	  
