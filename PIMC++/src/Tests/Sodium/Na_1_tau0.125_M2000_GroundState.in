double tau=0.125;

Section (Parallel)
{
  int ProcsPerClone = 1;
}


// This system contains 16 sodium atoms at T = 2210 *1143/2000 Kevin 
// and experimental lattice constant of 8.003 bohr
// Note:  energy per isolated atom for this PH is -0.1950864451427349 hartrees.
Section (System)
{
  int NumTimeSlices=2000;
  Array<bool,1> IsPeriodic(3) = [true,true,true];
  Array<double,1> Box(3)=[64.006, 64.006, 64.006];
  double kCutoff = 0.25;
  Section (Particles)
  {
    Section (Species)
    {
      string Name="Na";
      string Type="Na+";
      double lambda=0.0; 
      string Statistics="BOLTZMANNON";
      string NodeType = "NONE";
      int NumParticles=1;
      int NumDim=3;
      string InitPaths="FIXED";
      Array<double,2> Positions(1,3) = [0.0, 0.0, 0.0];
    }
    Section (Species)
    {
      string Name="eup";
      string Type = "e";
      string Statistics = "BOLTZMANNON";
      double lambda=0.5; 
      string NodeType = "FREE";
      int NumParticles=1;
      int NumDim=3;
      string InitPaths="LEVIFLIGHT";
      Array<double,2> Positions(1,3)=
        [ 0.1, 0.0, 0.0 ];
      
    }
  }
}


Section (Action)
{
  int MaxLevels = 5;
  bool UseRPA = false;
  int NumBreakupKnots = 20;
  int NumImages = 1;
  Array<string,1> PairActionFiles(3) = 
    ["Na-Na.PairAction",
     "/localscratch/esler/NaPH_US_April11_05.exfit.beta8.0.h5",
     "e-e_beta8.0.coulombBCfit.h5"];
}

Section (Observables)
{
  string OutFileBase = "/localscratch/esler/NaRuns/Na_1_tau0.125_M2000_BiggerBox";
  Section (Observable) {
    string type = "Energy";
    string Name = "Energy";
    int freq=20;
    int dumpFreq=100;
    
  }
  Section (Observable) {
    string type = "PairCorrelation";
    string Name = "Na-eup";
    string Species1 = "Na";
    string Species2 = "eup";
    int freq=20;
    int dumpFreq=100;
    Section (Grid) {
      string type = "Linear";
      int NumPoints = 200;
    }
  }
  Section (Observable) {
    string type = "PathDump";
    string Name = "PathDump";
    int dumpFreq = 2000;
  }
}


Section (Moves) {
  Section (Move) {
    string type="BisectionBlock";
    string name="eupBlock";
    //string PermuteType="TABLE";
    string PermuteType="NONE";
    Array<double,1> Gamma(4) = [1.0, 1.0, 1.0, 1.0];
    int NumLevels=5;
    string Species = "eup";
    int StepsPerBlock=8;
  }
  Section (Move) {
    string type="ShiftMove";
    string name="Shift";
  }
}

 


Section (Algorithm) {
  ////////////////////////
  // Equilibration loop //
  ////////////////////////
  Section (Loop) { 
    int steps=1000;
    Section (Loop){
      int steps=1;
      Section (Loop){
        int steps=2;
        Section (Move) { string name="eupBlock";} 
      }
      Section (Move) { string name = "Shift"; }
    }
  }

  Section (Loop) { //Start Accumulating observables
    int steps=6000000;
    Section (Loop){
      int steps=1;
      Section (Loop){
        int steps=2;
        Section (Move) { string name="eupBlock";} 
      }
      Section (Move) { string name = "Shift"; }
    }
    Section (Observe) { string name = "Na-eup";}
    Section (Observe) { string name = "Energy"; }
    Section (Observe) { string name = "PathDump"; }
  }
}




	  
