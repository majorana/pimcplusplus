double tau = 500.0;

Section (System)
{
  int NumTimeSlices=50;
  Array<bool,1> IsPeriodic(3) = [true,true,true];
  //Array<double,1> Box(3)=[974.87, 974.87, 974.87];
  Array<double,1> Box(3)=[1218.6, 1218.6, 1218.6];
  //Array<double,1> Box(3)=[7311.5, 7311.5, 7311.5];
  //Array<double,1> Box(3)=[1200.0, 1200.0, 1200.0];
  double kCutoff = 0.03;
  Section (Particles)
  {
    Section (Species)
    {
      string Name="e";
      double lambda=0.5;
      string Type="FERMION";
      int NumParticles=54;
      int NumDim=3;
      string InitPaths="BCC";
    }
  }
}

Section (Action)
{ 
  int MaxLevels = 5;
  bool UseRPA = true;
  Array<string,1> PairActionFiles(1) = [ "e-e_beta8000b.coulombBC.h5" ];
  int NumBreakupKnots = 20;
}

Section (Observables)
{
  string OutFile = "OCP_RPA_B_tau500.h5";
  Section (Observable)
    {
      string type = "Energy";
      string Name = "Energy";
      string Description="Total Energy";
      int freq = 2;
      int dumpFreq = 500;
    }
  Section (Observable)
    {
      string type = "PairCorrelation";
      string Name = "PC";
      string Species1 = "e";
      string Species2 = "e";
      string Description="Electron-Electron Pair Correlation";
      int freq = 40;
      int dumpFreq=700;
      Section (Grid)
	{
	  string type = "Linear";
	  double start = 0.0;
	  double end = 609.3;
	  int NumPoints = 100;
	}
    }
  Section (Observable)
    {
      string type = "StructureFactor";
      string Name = "Sk";
      string Species = "e";
      string Description="Electron-Electron Structure Factor";
      int freq = 4;
      int dumpFreq=700;
    }
  Section (Observable)
    {
      string type = "PathDump";
      string Name = "PathDump";
    }
}

	  
Section (Moves) {
  Section (Move)
    {
      string type="Bisection";
      string name="eBisection";
      
      Array<int,1> ActiveSpecies(1)=[0]; //This needs to be prettier input
      ///here should be electron
      int NumParticlesToMove=1;
      int StartTimeSlice=0; //Not sure if this is always set the same now?
      string NumLevels="Max";
    }
  Section (Move)
    {
      string type="ShiftMove";
      string name="electronShift";
    }
  Section (Move)
    {
      string type="PrintMove";
      string name="Loop1";
      string toprint="This is loop1";
    }
  Section (Move)
    {
      string type="PrintMove";
      string name="Loop2";
      string toprint="This is loop2";
    }
  Section (Move)
    {
      string type="PrintMove";
      string name="Loop3";
      string toprint="This is loop3";
    }
  Section (Move)
    {
      string type="CycleBlock";
      string name="BlockCycleMove";
      int NumLevels=3;
      Array<double,1> Gamma(4) = [1.0, 1.0, 1.0, 0.0];
      double epsilon=0.0;
      int Steps=9;
      string Species="e";
    }
}



Section (Algorithm)
{
//  Section (Loop) {
//    int steps = 10000;
//    Section (Observe) { string name = "PathDump"; }
//  }
  Section (Loop){ // Before Accumulating Observables
    int steps=1000; 
    Section (Loop){ 
      int steps=3;
      //Section (Move) {string name="BlockCycleMove";} 
      Section (Move)   {string name="eBisection";}
    }
    Section (Move)     {string name = "electronShift";}
  }
  Section (Loop){ //Start Accumulating observables
    int steps=3000;
    Section (Loop) {
      int steps = 1000;
      Section (Loop) {
        int steps = 10;
        Section (Loop){
          int steps=3;
          //Section (Move) {string name="BlockCycleMove";} 
          Section (Move) {string name="eBisection";} 
        }
        Section (Move) {string name = "electronShift"; }
      }
      Section (Observe) {string name = "PC";}
      Section (Observe) {string name = "Sk";}
      Section (Observe) {string name = "Energy";}
    }
    //Section (Observe)   {string name = "PathDump";}
  }
}
